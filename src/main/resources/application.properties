# Database
spring.datasource.url=jdbc:mysql://localhost:3306/mercadolibrewannabe?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=supervisor
# spring.datasource.initialization-mode=always

# API Config server.servlet.context-path=/api
# TODO: -> Problem on Swagger and SecurityLayer

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=10

# JWT
jwt.expiration.local=86400000